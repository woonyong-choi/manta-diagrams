import {mkdir, writeFile} from 'node:fs/promises';

const root = process.env.SITE_BASE || 'https://woonyong-choi.github.io/manta-diagrams/';
const gh = 'https://github.com/woonyong-choi/';
export const products = [
  {slug:'diagrams', name:'Manta Diagrams', short:'Diagrams', eyebrow:'Give an idea a clear shape', title:'Read the diagram.<br>Keep the source.', description:'A focused Mermaid viewer for technical notes. Make room for long labels, inspect a connection and take an SVG into your next document.', install:gh+'manta-diagrams/releases', action:'Get the plugin', demo:root+'try/', demoLabel:'Try your Mermaid', repo:'manta-diagrams', id:'manta-diagrams', version:'0.1.0',
    poster:'docs/assets/manta-diagrams-poster.png', media:'docs/assets/manta-diagrams-intro', caption:'Manta Diagrams 0.1.0. The introduction uses the shared browser viewer; Obsidian installation is checked separately.',
    steps:[['Start with Mermaid','Keep a normal Mermaid block in your note. Open the diagram under the cursor from the command palette.'],['Make room for the detail','Fit the whole diagram, zoom to a label or drag to follow a long connection. The source is always available below.'],['Choose a layout when it helps','Use a Manta layout comment for a loop or a structured flow. Switch back to standard Mermaid and save an SVG when the view is ready.']],
    note:'Ordinary Mermaid remains handled by Mermaid. Special layouts are optional; unsupported layouts fall back with an explanation. The viewer does not edit notes or call an AI service.',
    together:'Explain a process with Diagrams, follow its supporting notes with Graph, then try the example in Code Blocks. Calendar helps you return to the dated review.'},
  {slug:'graph',name:'Manta Graph',short:'Graph',eyebrow:'Follow the thread',title:'The next note<br>is already linked.',description:'Move through the links in your current note. See what connects, preview the next step and return to the outline when you want a reading order.',install:'https://community.obsidian.md/plugins/linked-graph',action:'Install in Obsidian',demo:gh+'obsidian-navigator-demo-vault/releases/latest',demoLabel:'Try the sample vault',repo:'manta-graph',id:'linked-graph',version:'1.6.14',poster:'docs/assets/linked-graph-runtime.png',media:'docs/assets/manta-graph-intro',caption:'Real Obsidian captures from Graph 1.6.12. These public sample screens retain the earlier name.',
    steps:[['Write the links you mean','Link a note to an explanation, a lesson or the next step. Graph follows these existing links without inventing new relationships.'],['Open the current note’s graph','Inspect nearby notes, move the graph and preview the next connection before opening it.'],['Read in order','Switch to Outline to follow links in the order they appear in the source note.']],
    note:'Works offline without an account. Manta Graph reads links and does not edit notes. Unresolved links do not become routes.',together:'Use Graph to move between a diagram, its explanation and a runnable example. Link to the dated review so Calendar can help you find it again.'},
  {slug:'code-blocks',name:'Manta Code Blocks',short:'Code Blocks',eyebrow:'An example you can try',title:'Read it.<br>Change it. Run it.',description:'Keep a working example beside the explanation. Edit the code, run it and inspect the result without leaving your note.',install:'https://community.obsidian.md/plugins/runnable-code-blocks',action:'Install in Obsidian',demo:'https://woonyong-choi.github.io/manta-code-blocks/',demoLabel:'Try the live editor',repo:'manta-code-blocks',id:'runnable-code-blocks',version:'0.7.9',poster:'docs/assets/runnable-web-preview.png',media:'docs/assets/manta-code-blocks-intro',caption:'Real browser demo captures from Code Blocks 0.7.3. These examples retain the earlier name.',
    steps:[['Add a runnable block','Write a run-javascript block in a note, switch to Reading view and select Run.'],['Change one thing','Edit the example and run it again. The output stays next to the code so you can compare what changed.'],['Keep the useful version','Copy the edited code or use Reset to restore the original example.']],
    note:'Code runs only when you choose Run. Seven browser fences need no account or server. Other languages use an optional local companion or a named remote provider. Remote execution is enabled by default and can be disabled in settings.',together:'Use Diagrams to explain the flow, Code Blocks to try it and Graph to connect the result to the rest of your notes. Date the experiment when you want it to appear in Calendar.'},
  {slug:'calendar',name:'Manta Calendar',short:'Calendar',eyebrow:'Find it by the day',title:'A date is a way<br>back to the note.',description:'Bring dated notes into a calendar. See the month, inspect a day and open the original note with one click.',install:'https://community.obsidian.md/plugins/link-calendar',action:'Install in Obsidian',demo:gh+'obsidian-navigator-demo-vault/releases/latest',demoLabel:'Try the sample vault',repo:'manta-calendar',id:'link-calendar',version:'3.6.6',poster:'docs/media/link-calendar-overview.png',media:'docs/assets/manta-calendar-intro',caption:'Real Obsidian captures from Calendar 3.6.0. Google authentication is not shown.',
    steps:[['Give a note a date','Add a dated line to an ordinary note, or configure a source folder with your date properties.'],['See what belongs to the day','Open Manta Calendar and choose a date. The day panel lists the matching notes.'],['Return to the source','Select an entry to open the original note. The local calendar needs no account.']],
    note:'Google sync is optional and uses a dedicated calendar. Two-way sync is opt-in; source permissions and conflict checks apply. See the setup guide before connecting an account.',together:'Return to a dated experiment in Calendar, follow its supporting notes in Graph and reopen the diagram or code example where you left it.'},
];
const esc = text => String(text).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const asset = (p,path) => `https://raw.githubusercontent.com/woonyong-choi/${p.repo}/main/${path}`;
const header = name => `<header class="site-header"><a class="brand" href="${root}manta/">Manta <span>${name}</span></a><nav aria-label="Main"><a href="${root}manta/">The family</a><a href="${root}manta/#in-development">What’s next</a><a href="${gh}manta-diagrams">GitHub</a></nav></header>`;
const footer = () => `<footer><span>Made by Woonyong Choi · Open-source tools for Obsidian</span><a href="${root}manta/#in-development">Manta is in development</a></footer>`;
const shell = (title,description,body) => `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><meta name="description" content="${esc(description)}"><meta name="theme-color" content="#f8f7f3"><title>${esc(title)}</title><link rel="stylesheet" href="${root}style.css"></head><body>${body}</body></html>`;
const upcoming = `<aside id="in-development" class="upcoming"><p class="eyebrow">In development · Not released</p><h2>Manta is taking shape.</h2><p>I’m building Manta to turn source material into a personal wiki you can keep adding to. The aim is to bring explanations, links and references together while keeping the result in ordinary Markdown.</p><p>The four tools here stand on their own. Their connection to Manta, shared AI tools and the full wiki workflow are still in development.</p></aside>`;
const cards = omit => `<div class="family-grid">${products.filter(p=>p.slug!==omit).map((p,i)=>`<a class="product-card" href="${root}${p.slug==='diagrams'?'':p.slug+'/'}"><span class="number">0${i+1} / ${p.short}</span><h3>${p.name}</h3><p>${p.description}</p><span class="more">Explore ${p.short} →</span></a>`).join('')}</div>`;
export async function buildPages() {
  for(const p of products) {
    const dir = p.slug==='diagrams'?'dist':`dist/${p.slug}`; await mkdir(dir,{recursive:true});
    const body = `${header(p.short)}<main><section class="hero"><div><p class="eyebrow">${p.eyebrow}</p><h1>${p.title}</h1></div><div><p class="lead">${p.description}</p><div class="actions"><a class="button" href="${p.install}">${p.action}</a><a href="${p.demo}">${p.demoLabel} →</a></div><p class="quiet" style="margin-top:18px">${p.name} ${p.version} · ${p.slug==='diagrams'?'First release · Community submission in preparation':'Available for Obsidian'}</p></div></section>
    <figure class="media"><video controls playsinline preload="metadata" poster="${asset(p,p.poster)}" aria-label="${p.name} introduction"><source src="${asset(p,p.media+'.mp4')}" type="video/mp4"></video><figcaption>${p.caption} <a href="${asset(p,p.media+'.gif')}">GIF version</a></figcaption></figure>
    <section class="section section-grid"><div><p class="eyebrow">In your next note</p><h2>A small start.<br>A useful result.</h2></div><ol class="steps">${p.steps.map(([title,text])=>`<li><h3>${title}</h3><p>${text}</p></li>`).join('')}</ol></section>
    <section class="section section-grid"><h2>Know what happens.</h2><div><p>${p.note}</p><div class="inline-links"><a href="${gh+p.repo}#readme">Read the guide</a><a href="${gh+p.repo}/issues">Report a problem</a><a href="${gh+p.repo}/blob/main/ROADMAP.md">What’s planned</a></div></div></section>
    <section class="section"><p class="eyebrow">One note, a few useful views</p><h2>Let the work stay connected.</h2><p class="lead">${p.together}</p><p class="quiet">Use ordinary notes and links today. Automatic handoffs between the plugins are planned.</p>${cards(p.slug)}</section><section class="section">${upcoming}</section></main>${footer()}`;
    await writeFile(dir+'/index.html',shell(p.name,p.description,body));
  }
  await mkdir('dist/manta',{recursive:true});
  await writeFile('dist/manta/index.html',shell('Manta — tools for thoughtful notes','Explain, explore, try and revisit. Four small Obsidian tools for working with technical notes.',`${header('for Obsidian')}<main><section class="hero"><div><p class="eyebrow">Tools for thoughtful notes</p><h1>Good notes deserve<br>to be used.</h1></div><p class="lead">Explain an idea. Follow a connection. Try the example. Find it again. Four tools for the work you already keep in Obsidian.</p></section>${cards()}<section class="section section-grid"><h2>Start with the part<br>you need.</h2><div><p>Each plugin works on its own. Your notes and links provide the connection: a diagram explains a process, code tests it, the graph leads to related pages, and a date brings you back.</p><p class="quiet">No bundle installation is required. Shared setup and AI-assisted workflows are on the roadmap.</p></div></section><section class="section">${upcoming}</section></main>${footer()}`));
}
